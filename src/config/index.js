export const API = Object.freeze({
  HOST: "http://localhost:8000",
  V1_UPLOAD_SESSIONS_PATH: "/api/v1/upload-sessions",
  V1_UPLOAD_CHUNKS_PATH: "/api/v1/upload-chunks"
});

export const FILE = Object.freeze({
  CHUNK_BYTE_SIZE: 1000
});
