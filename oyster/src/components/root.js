import React from "react";

import "./app.css";

import Header from "./header";

const Root = () => (
  <div className="App">
    <Header />
    <button className="App-intro">Upload a file.</button>
  </div>
);

export default Root;
